# memo_test.ver
메모장
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>메모장</title>
    <style>
        body { font-family: Arial, sans-serif; }
        textarea { width: 100%; height: 100px; }
        button { margin: 5px; padding: 10px; }
        ul { list-style: none; padding: 0; }
        li { cursor: pointer; padding: 5px; border: 1px solid #ddd; margin: 5px; }
        .active { background-color: yellow; } /* 선택된 메모 강조 */
    </style>
</head>
<body>
    <h2>메모장</h2>
    <textarea id="memoInput" placeholder="메모를 입력하세요"></textarea>
    <button onclick="saveMemo()">저장</button>
    <button onclick="deleteMemo()">삭제</button> <!-- 삭제 버튼 추가 -->
    <ul id="memoList"></ul>
    <script>
        let activeIndex = null; // 현재 선택된 메모의 인덱스

        function saveMemo() {
            const memoInput = document.getElementById("memoInput").value.trim();
            let memos = JSON.parse(localStorage.getItem("memos")) || [];
            
            if (activeIndex !== null) { 
                if (memoInput) { 
                    memos[activeIndex] = memoInput; // 기존 메모 덮어쓰기
                } else {
                    memos.splice(activeIndex, 1); // 빈 값이면 삭제
                }
            } else if (memoInput) {
